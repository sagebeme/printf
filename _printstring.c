#include "main.h"

/**
 * _putstring - prints string
 * @s: the pointer passes through this
 *
 * Description: puts the string and returns the length
 * Return: the string length
 */

int _putstring(char *)
{
	int i = 0;

	while (*s != '\0')
	{
		_putchar(*s);
		s++
		i++
	}
	return (i);
}
